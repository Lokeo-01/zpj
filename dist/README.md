# Personal Portfolio

Direct static import package for Nuwa AI.

- Entry: `index.html`
- Build command: `npm run build`
- Run command: `npm run dev`
- Assets are in root folders: `assets`, `works`, `icons`, `optimized`, `backgrounds`

This version does not use iframe or nested `/portfolio` paths.
